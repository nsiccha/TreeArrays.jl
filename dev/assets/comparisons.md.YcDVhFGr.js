import{_ as a,o as s,c as t,ao as i}from"./chunks/framework.DIr9xbwu.js";const g=JSON.parse('{"title":"Comparisons","description":"","frontmatter":{},"headers":[],"relativePath":"comparisons.md","filePath":"comparisons.md","lastUpdated":null}'),r={name:"comparisons.md"};function n(l,e,o,d,h,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="Comparisons" tabindex="-1">Comparisons <a class="header-anchor" href="#Comparisons" aria-label="Permalink to &quot;Comparisons {#Comparisons}&quot;">​</a></h1><p>TreeArrays sits next to two packages people reasonably ask about: <strong>DimensionalData.jl</strong>, which also gives arrays named dimensions, and <strong>FlexiChains.jl</strong>, which holds MCMC output. This page says what is genuinely different, what is borrowed, and — for FlexiChains — what an integration would look like.</p><h2 id="vs-dd" tabindex="-1">TreeArrays vs. DimensionalData <a class="header-anchor" href="#vs-dd" aria-label="Permalink to &quot;TreeArrays vs. DimensionalData {#vs-dd}&quot;">​</a></h2><p>The overlap is heavy and deliberate: named dims, dim-aware reductions, selectors, a Tables.jl bridge. TreeArrays also <strong>borrows DD&#39;s central good idea</strong> — dim <em>identity</em> lives in the type, so axis resolution happens at compile time and reductions stay type-stable.</p><p>Two differentiators drove building something separate.</p><h3 id="a-Lazy-assembly-—-not-lazy-compute" tabindex="-1">(a) Lazy <em>assembly</em> — not lazy compute <a class="header-anchor" href="#a-Lazy-assembly-—-not-lazy-compute" aria-label="Permalink to &quot;(a) Lazy _assembly_ — not lazy compute {#a-Lazy-assembly-—-not-lazy-compute}&quot;">​</a></h3><p>DimensionalData&#39;s operations are eager <strong>and</strong> they materialize: a reduction produces a new <code>DimArray</code> holding real memory, and the array you reduce is a real array to begin with.</p><p>TreeArrays keeps the arithmetic eager and makes the <strong>assembly</strong> lazy. The cross-product of categorical and virtual axes — draws × subjects × doses × placebo × parameter-space — is described <em>structurally</em> and never allocated. A reduction materializes one block, runs the kernel on it, discards it, and moves to the next.</p><p>This is a narrow claim, and it is worth being precise about what it is <em>not</em>:</p><blockquote><p>TreeArrays is <strong>not</strong> a deferred-compute package. There is no <code>BroadcastArray</code>, no compute graph, no per-element closure, no recompute-on-reaccess. Transforms and summary statistics run <em>now</em>, in tight type-stable loops. The only thing that is lazy is <em>forming the big combined matrix</em>.</p></blockquote><p>The performance enemies in this domain are allocating that combined matrix and poor per-block kernel quality — not the cost of individual evaluations. A deferred-compute design attacks the wrong one and usually loses.</p><h3 id="b-Ragged-axes" tabindex="-1">(b) Ragged axes <a class="header-anchor" href="#b-Ragged-axes" aria-label="Permalink to &quot;(b) Ragged axes {#b-Ragged-axes}&quot;">​</a></h3><p>DimensionalData is fundamentally <strong>rectangular</strong>. Every element of a dimension has the same shape underneath it.</p><p>The data this package was written for is not: 179 subjects with different numbers of measurements, and a <em>separate</em> irregular dose history per subject. In TreeArrays that is a <a href="/TreeArrays.jl/dev/api#TreeArrays.TreeRaggedArray"><code>TreeRaggedArray</code></a> — an outer axis of sub-trees with different shapes — and the <em>same</em> <code>mapslices(f, X; dims = :time)</code> call runs on it, because rectangular is just ragged with uniform offsets. See <a href="/TreeArrays.jl/dev/ragged#Ragged-data">Ragged data</a>.</p><h3 id="The-honest-cost:-a-TreeData-is-not-an-AbstractArray" tabindex="-1">The honest cost: a <code>TreeData</code> is not an <code>AbstractArray</code> <a class="header-anchor" href="#The-honest-cost:-a-TreeData-is-not-an-AbstractArray" aria-label="Permalink to &quot;The honest cost: a \`TreeData\` is not an \`AbstractArray\` {#The-honest-cost:-a-TreeData-is-not-an-AbstractArray}&quot;">​</a></h3><p>This is the trade the table below cannot express. A <code>DimArray</code> <strong>is</strong> an <code>AbstractArray</code>, so it drops into the entire Julia array ecosystem for free — every function typed on <code>AbstractArray</code>, every broadcast, every linear-algebra call.</p><p>A <code>TreeData</code> is deliberately <strong>not</strong> an <code>AbstractArray</code> subtype, because a record node and a ragged node are not arrays in any honest sense, and pretending otherwise would make the dispatch lie. The price is that crossing into array-land is an explicit step: <a href="/TreeArrays.jl/dev/api#TreeArrays.TreeActualArray"><code>TreeActualArray(X)</code></a> — a lazy, zero-copy view that also keeps the labels — or <code>parent(X)</code> / <code>collect(X)</code>. See <a href="/TreeArrays.jl/dev/tables#actual-array">Feeding a numeric-array API</a>.</p><p>If your data is rectangular and you want it to behave like an array everywhere, that is a genuine reason to prefer DimensionalData.</p><h3 id="Side-by-side" tabindex="-1">Side by side <a class="header-anchor" href="#Side-by-side" aria-label="Permalink to &quot;Side by side {#Side-by-side}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">DimensionalData.jl</th><th style="text-align:right;">TreeArrays.jl</th></tr></thead><tbody><tr><td style="text-align:right;">Core type</td><td style="text-align:right;"><code>DimArray &lt;: AbstractArray</code></td><td style="text-align:right;"><code>TreeData</code>, deliberately not an array</td></tr><tr><td style="text-align:right;">Shape</td><td style="text-align:right;">rectangular</td><td style="text-align:right;">rectangular <strong>or ragged</strong></td></tr><tr><td style="text-align:right;">Nesting</td><td style="text-align:right;"><code>DimStack</code> of co-dimensional layers</td><td style="text-align:right;">a recursive <strong>tree</strong>: leaves, records, ragged nodes</td></tr><tr><td style="text-align:right;">Ops</td><td style="text-align:right;">eager, materializing</td><td style="text-align:right;">eager compute, <strong>lazy assembly</strong></td></tr><tr><td style="text-align:right;">Dim identity in the type</td><td style="text-align:right;">yes</td><td style="text-align:right;">yes (borrowed)</td></tr><tr><td style="text-align:right;">Selectors</td><td style="text-align:right;">rich (<code>At</code>, <code>Near</code>, <code>Between</code>, …)</td><td style="text-align:right;"><code>selectdim</code> — Regex / predicate / mask / indices</td></tr><tr><td style="text-align:right;">Scope</td><td style="text-align:right;">general-purpose named arrays; the base of a GIS/raster ecosystem</td><td style="text-align:right;">a narrow <strong>postprocessing / summarization</strong> layer</td></tr><tr><td style="text-align:right;">Maturity</td><td style="text-align:right;">mature, widely used</td><td style="text-align:right;"><code>v0.1.0</code>, unregistered, young</td></tr></tbody></table><h3 id="Will-TreeArrays-supersede-DimensionalData?" tabindex="-1">Will TreeArrays supersede DimensionalData? <a class="header-anchor" href="#Will-TreeArrays-supersede-DimensionalData?" aria-label="Permalink to &quot;Will TreeArrays supersede DimensionalData? {#Will-TreeArrays-supersede-DimensionalData?}&quot;">​</a></h3><p><strong>Genuinely open, and stated as such rather than promised.</strong> The original design note lists &quot;build on DD vs. standalone&quot; as the key unresolved question, and the standalone route was taken to get the two differentiators above — not because DD was found wanting on its own ground.</p><p>What is intended: that TreeArrays eventually covers everything its own users currently need from a named-dimension array, so a PKPD postprocessing pipeline never has to reach for both. What is <strong>not</strong> claimed: that it will subsume DD&#39;s scope. DD anchors a much broader ecosystem (rasters, climate data cubes, geospatial stacks) whose requirements are not on this package&#39;s roadmap, and it gives up nothing to be an <code>AbstractArray</code>, which TreeArrays gives up deliberately. A package that is not an array cannot strictly supersede one that is.</p><p>Treat the two as overlapping-but-distinct until this page says otherwise.</p><h2 id="vs-fc" tabindex="-1">TreeArrays and FlexiChains <a class="header-anchor" href="#vs-fc" aria-label="Permalink to &quot;TreeArrays and FlexiChains {#vs-fc}&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">This section is a sketch, not a shipped integration</p><p>TreeArrays has <strong>no</strong> FlexiChains dependency, extension, or tested bridge today, and the code below has not been run against FlexiChains. It describes the <em>shape</em> such an integration would take, so the design is written down somewhere. Check the FlexiChains documentation for its current API before relying on any spelling here.</p></div><p>FlexiChains.jl and TreeArrays solve <strong>adjacent, non-overlapping</strong> problems.</p><p>FlexiChains is a <strong>container for sampler output</strong>: it holds what a sampler produced, over iterations and chains, and its distinguishing feature is that a &quot;parameter&quot; need not be a <code>Float64</code> — a draw can be a vector, a matrix, or a custom struct, keyed by the variable names the model actually used. That is a storage and identity problem.</p><p>TreeArrays is a <strong>summarization layer</strong>: given draws that already exist, name their axes and reduce over them, without ever building the fat intermediate frame. That is a reduction problem.</p><p>Neither replaces the other, and the seam between them is clean.</p><h3 id="The-natural-bridge" tabindex="-1">The natural bridge <a class="header-anchor" href="#The-natural-bridge" aria-label="Permalink to &quot;The natural bridge {#The-natural-bridge}&quot;">​</a></h3><p>The two type systems line up almost directly, because a chain object and a <code>TreeData</code> describe the same logical thing — a <code>(draw, chain)</code> grid of keyed values:</p><table tabindex="0"><thead><tr><th style="text-align:right;">FlexiChains</th><th style="text-align:right;">TreeArrays</th></tr></thead><tbody><tr><td style="text-align:right;">the iteration dimension</td><td style="text-align:right;">an unlabelled <code>:draw</code> axis</td></tr><tr><td style="text-align:right;">the chain dimension</td><td style="text-align:right;">an unlabelled <code>:chain</code> axis</td></tr><tr><td style="text-align:right;">one scalar parameter key</td><td style="text-align:right;">a field of a <a href="/TreeArrays.jl/dev/api#TreeArrays.TreeNamedTuple"><code>TreeNamedTuple</code></a> record, or a coordinate on a <code>:param</code> axis</td></tr><tr><td style="text-align:right;">one array-valued parameter key</td><td style="text-align:right;">a nested <code>TreeData</code> leaf with its own inner axes</td></tr><tr><td style="text-align:right;">keys whose per-draw values differ in size</td><td style="text-align:right;">a <a href="/TreeArrays.jl/dev/api#TreeArrays.TreeRaggedArray"><code>TreeRaggedArray</code></a> node</td></tr></tbody></table><p>So a conversion would build a record whose fields are the chain&#39;s keys, over shared <code>:draw</code> and <code>:chain</code> axes:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SKETCH — not a supported API. Adapt the accessors to FlexiChains&#39; actual surface.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> TreeArrays</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TreeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chain</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FlexiChains.FlexiChain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fields </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (; (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Symbol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _as_leaf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chain, k) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> k </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> keys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chain))</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    TreeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:param</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fields, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:draw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a scalar key becomes a plain (draw, chain) matrix; an array-valued key becomes</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a nested TreeData carrying its own inner axes</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_as_leaf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chain, k) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ...</span></span></code></pre></div><p>Once that exists, everything on the rest of this site applies unchanged:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> TreeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chain)                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># zero-copy where the backing allows</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mu                                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># read one key back, still labelled</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">quantile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(post, (lower </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.025</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, median </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, upper </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.975</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); dims </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:draw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TreeActualArray</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(post)                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → ess / rhat, no copy</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Tables</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">columns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(post)                         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → any plotting layer, no DataFrame</span></span></code></pre></div><p>Three things make the pairing more than cosmetic:</p><ul><li><p><strong>Pooled <code>(draw, chain)</code> reductions are one sorted pass over the whole bag</strong> — exactly the quantity a pooled credible interval wants — and TreeArrays computes it identically whether the chains are one dense 3-D array or a <em>ragged</em> <code>:chain</code> axis of separately-allocated per-chain matrices. Chains that were sampled independently, possibly memory-mapped, never need <code>hcat</code>ing to be summarized together. See <a href="/TreeArrays.jl/dev/ragged#Ragged-data">Ragged data</a>.</p></li><li><p><strong>FlexiChains&#39; arbitrary-type values are what TreeArrays&#39; record and ragged nodes are for.</strong> A key whose draws are vectors of <em>differing</em> length is a ragged node, not a padding problem.</p></li><li><p><strong><code>TreeActualArray</code></strong> promotes a record axis to a real array dimension, so a record-of-per-parameter-matrices feeds <code>ess</code>/<code>rhat</code> with no copy and no parallel plain-array builder.</p></li></ul><h3 id="Where-this-would-live" tabindex="-1">Where this would live <a class="header-anchor" href="#Where-this-would-live" aria-label="Permalink to &quot;Where this would live {#Where-this-would-live}&quot;">​</a></h3><p>Not in TreeArrays&#39; core. A hard dependency on any modelling-ecosystem package is a non-starter here — the core&#39;s dependencies are <code>Statistics</code> and <code>Tables</code>, and that is intentional. The bridge belongs in a <strong>package extension</strong>, on whichever side is willing to own it, exactly as the <code>NaNStatistics</code> extension does for <a href="/TreeArrays.jl/dev/reductions#nan-safe-quantiles"><code>nanquantile</code></a>.</p><p>If you want this, <a href="https://github.com/nsiccha/TreeArrays.jl/issues" target="_blank" rel="noreferrer">open an issue</a> — knowing which direction the conversion is actually needed in is the missing input, not the implementation.</p>`,42)])])}const y=a(r,[["render",n]]);export{g as __pageData,y as default};
