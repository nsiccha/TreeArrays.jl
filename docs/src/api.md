```@meta
CurrentModule = TreeArrays
```

# API

Everything `TreeArrays` exports, plus the `Base` and `Statistics` methods it
extends.

```@index
```

```@autodocs
Modules = [TreeArrays]
Order = [:module, :type, :macro, :function]
```
